import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import api from '../../services/api.js';
import Sidebar from '../../components/common/Sidebar.jsx';
import Topbar from '../../components/common/Topbar.jsx';
import Breadcrumbs from '../../components/common/Breadcrumbs.jsx';
import Modal from '../../components/common/Modal.jsx';
import Button from '../../components/common/Button.jsx';
import StatusBadge from '../../components/common/StatusBadge.jsx';
import Pagination from '../../components/common/Pagination.jsx';
import LoadingState from '../../components/common/LoadingState.jsx';
import ErrorState from '../../components/common/ErrorState.jsx';
import Toast from '../../components/common/Toast.jsx';
import { cleanParams } from '../../utils/cleanParams.js';

export default function SuperAdminPage({ user, onExit }) { const navigate = useNavigate(); const [stats, setStats] = useState(null); const [tenants, setTenants] = useState([]); const [pagination, setPagination] = useState({ page: 1, totalPages: 1 }); const [filters, setFilters] = useState({ search: '', plan: '', status: '' }); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [message, setMessage] = useState(''); const [selected, setSelected] = useState(null); const [mobileOpen, setMobileOpen] = useState(false); async function load(page = 1) { setLoading(true); setError(''); try { const [statsResponse, tenantResponse] = await Promise.all([api.get('/super/stats'), api.get('/super/tenants', { params: { ...filters, page, pageSize: 10 } })]); setStats(statsResponse.data.data); setTenants(tenantResponse.data.data?.items || []); setPagination(tenantResponse.data.data?.pagination || { page, totalPages: 1 }); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not load platform data.'); } finally { setLoading(false); } } useEffect(() => { load(1); }, [filters.search, filters.plan, filters.status]); function notify(text) { setMessage(text); window.setTimeout(() => setMessage(''), 2400); } async function changeStatus(tenant) { const action = tenant.isActive ? 'deactivate' : 'reactivate'; try { await api.patch(`/super/tenants/${tenant.id}/${action}`); notify(`Tenant ${action}d successfully`); setSelected(null); await load(pagination.page); } catch (requestError) { setError(requestError.response?.data?.error?.message || `Could not ${action} tenant.`); } } async function viewTenant(tenant) { try { const response = await api.get(`/super/tenants/${tenant.id}`); setSelected(response.data.data); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not load tenant details.'); } } return <div className="app"><Sidebar active="Overview" role="super_admin" open={mobileOpen} onClose={() => setMobileOpen(false)} onNavigate={() => navigate('/super-admin')} onLogout={onExit} /><div className="app-content"><Topbar user={user} onMenu={() => setMobileOpen(true)} onNotifications={() => navigate('/notifications')} onSettings={() => navigate('/super-admin')} onLogout={onExit} /><main className="dashboard-main"><Breadcrumbs items={[{ label: 'Platform' }, { label: 'Super Admin' }]} /><div className="page-heading"><div><div className="eyebrow">Platform administration</div><h1>Super Admin</h1><p>Manage WorkNest tenants and platform health.</p></div></div>{error && <ErrorState message={error} onRetry={() => load(pagination.page)} />}{loading && !error && <LoadingState label="Loading platform data…" />}{!loading && !error && <><div className="platform-stats"><Stat label="Total tenants" value={stats?.totalTenants} /><Stat label="Active tenants" value={stats?.activeTenants} tone="green" /><Stat label="Employees" value={stats?.totalEmployees} tone="blue" /><Stat label="Monthly revenue" value={`${stats?.currency || 'USD'} ${stats?.simulatedMonthlyRevenue || 0}`} tone="purple" /></div><div className="tenant-toolbar"><input value={filters.search} onChange={event => setFilters(current => ({ ...current, search: event.target.value }))} placeholder="Search companies or slugs" /><select value={filters.plan} onChange={event => setFilters(current => ({ ...current, plan: event.target.value }))}><option value="">All plans</option><option value="starter">Starter</option><option value="growth">Growth</option><option value="enterprise">Enterprise</option></select><select value={filters.status} onChange={event => setFilters(current => ({ ...current, status: event.target.value }))}><option value="">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div><div className="tenant-table-wrap"><table className="data-table"><thead><tr><th>Company</th><th>Plan</th><th>Employees</th><th>Departments</th><th>Status</th><th>Actions</th></tr></thead><tbody>{tenants.map(tenant => <tr key={tenant.id}><td><button className="table-link" onClick={() => viewTenant(tenant)}>{tenant.companyName}</button><small>{tenant.slug}</small></td><td>{tenant.plan}</td><td>{tenant.employeeCount}</td><td>{tenant.departmentCount}</td><td><StatusBadge status={tenant.isActive ? 'active' : 'inactive'} /></td><td className="row-actions"><button onClick={() => viewTenant(tenant)}>View</button><button onClick={() => changeStatus(tenant)}>{tenant.isActive ? 'Deactivate' : 'Reactivate'}</button></td></tr>)}</tbody></table>{!tenants.length && <div className="table-empty">No tenants found.</div>}</div><Pagination page={pagination.page || 1} totalPages={pagination.totalPages || 1} onChange={load} /></>}</main></div><Modal open={Boolean(selected)} title={selected?.companyName || 'Tenant details'} description="Tenant overview and workspace configuration." onClose={() => setSelected(null)}>{selected && <div className="tenant-detail"><div><strong>Slug</strong><span>{selected.slug}</span></div><div><strong>Plan</strong><span>{selected.plan}</span></div><div><strong>Employees</strong><span>{selected.employeeCount} / {selected.employeeLimit}</span></div><div><strong>Departments</strong><span>{selected.departmentCount}</span></div><div><strong>Timezone</strong><span>{selected.settings?.timezone || '—'}</span></div><div><strong>Work hours</strong><span>{selected.settings?.workStartTime?.slice(0, 5)} – {selected.settings?.workEndTime?.slice(0, 5)}</span></div><Button className="full" onClick={() => changeStatus(selected)}>{selected.isActive ? 'Deactivate tenant' : 'Reactivate tenant'}</Button></div>}</Modal><Toast message={message} onClose={() => setMessage('')} /></div>; }
function Stat({ label, value, tone = 'violet' }) { return <div className={`platform-stat ${tone}`}><span>{label}</span><strong>{value ?? '—'}</strong></div>; }
