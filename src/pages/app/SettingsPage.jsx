import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import api from '../../services/api.js';
import Sidebar from '../../components/common/Sidebar.jsx';
import Topbar from '../../components/common/Topbar.jsx';
import Breadcrumbs from '../../components/common/Breadcrumbs.jsx';
import Button from '../../components/common/Button.jsx';
import LoadingState from '../../components/common/LoadingState.jsx';
import ErrorState from '../../components/common/ErrorState.jsx';
import Toast from '../../components/common/Toast.jsx';

const paths = { Overview: '/dashboard', People: '/employees', Attendance: '/attendance', 'Time off': '/leaves', Payroll: '/payroll', Departments: '/departments', Notifications: '/notifications', Settings: '/settings' };
export default function SettingsPage({ user, onExit }) { const navigate = useNavigate(); const role = user?.user?.role || 'admin'; const [company, setCompany] = useState({}); const [hours, setHours] = useState({}); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const [message, setMessage] = useState(''); const [mobileOpen, setMobileOpen] = useState(false); useEffect(() => { api.get('/settings').then(response => { const data = response.data.data || {}; setCompany(data.company || {}); setHours(data.workHours || {}); }).catch(requestError => setError(requestError.response?.data?.error?.message || 'Could not load settings.')).finally(() => setLoading(false)); }, []); async function saveCompany(event) { event.preventDefault(); setSaving(true); try { const form = new FormData(event.currentTarget); const response = await api.put('/settings', Object.fromEntries(form.entries())); setCompany(response.data.data.company); setMessage('Company profile saved'); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not save company profile.'); } finally { setSaving(false); } } async function saveHours(event) { event.preventDefault(); setSaving(true); try { const form = new FormData(event.currentTarget); const response = await api.patch('/settings/work-hours', Object.fromEntries(form.entries())); setHours(response.data.data.workHours); setMessage('Work-hour settings saved'); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not save work-hour settings.'); } finally { setSaving(false); } } const go = label => { setMobileOpen(false); navigate(paths[label] || '/dashboard'); }; if (loading) return <div className="app"><LoadingState label="Loading settings…" /></div>; return <div className="app"><Sidebar active="Settings" role={role} open={mobileOpen} onClose={() => setMobileOpen(false)} onNavigate={go} onLogout={onExit} /><div className="app-content"><Topbar user={user} onMenu={() => setMobileOpen(true)} onNotifications={() => navigate('/notifications')} onSettings={() => navigate('/settings')} onLogout={onExit} /><main className="dashboard-main"><Breadcrumbs items={[{ label: 'Workspace' }, { label: 'Settings' }]} /><div className="page-heading"><div><div className="eyebrow">Administration</div><h1>Company settings</h1><p>Manage your organization profile and attendance rules.</p></div></div>{error && <ErrorState message={error} onRetry={() => window.location.reload()} />} {!error && <div className="settings-grid"><section className="settings-card"><h2>Company profile</h2><p>Basic details shown across your workspace.</p><form onSubmit={saveCompany}><label>Company name<input name="companyName" required defaultValue={company.companyName || ''} /></label><label>Industry<input name="industry" defaultValue={company.industry || ''} /></label><label>Address<textarea name="address" defaultValue={company.address || ''} /></label><label>Logo URL<input name="logoUrl" type="url" defaultValue={company.logoUrl || ''} placeholder="https://..." /></label><Button type="submit" loading={saving}>Save profile</Button></form></section><section className="settings-card"><h2>Work hours</h2><p>These settings control timezone and late-arrival detection.</p><form onSubmit={saveHours}><label>Timezone<input name="timezone" required defaultValue={hours.timezone || 'Asia/Karachi'} /></label><label>Currency<input name="currency" maxLength="3" required defaultValue={hours.currency || 'PKR'} /></label><div className="form-grid"><label>Work starts<input name="workStartTime" type="time" required defaultValue={hours.workStartTime?.slice(0, 5) || '09:00'} /></label><label>Work ends<input name="workEndTime" type="time" required defaultValue={hours.workEndTime?.slice(0, 5) || '17:00'} /></label></div><label>Late threshold<input name="lateThreshold" type="time" required defaultValue={hours.lateThreshold?.slice(0, 5) || '09:15'} /></label><Button type="submit" loading={saving}>Save work hours</Button></form></section></div>} </main></div><Toast message={message} onClose={() => setMessage('')} /></div>; }
