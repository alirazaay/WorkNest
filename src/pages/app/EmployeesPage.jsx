import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import api from '../../services/api.js';
import Sidebar from '../../components/common/Sidebar.jsx';
import Topbar from '../../components/common/Topbar.jsx';
import Modal from '../../components/common/Modal.jsx';
import Button from '../../components/common/Button.jsx';
import Breadcrumbs from '../../components/common/Breadcrumbs.jsx';
import StatusBadge from '../../components/common/StatusBadge.jsx';
import Pagination from '../../components/common/Pagination.jsx';
import Toast from '../../components/common/Toast.jsx';
import ErrorState from '../../components/common/ErrorState.jsx';
import LoadingState from '../../components/common/LoadingState.jsx';
import { cleanParams } from '../../utils/cleanParams.js';

const paths = { Overview: '/dashboard', People: '/employees', Attendance: '/attendance', 'Time off': '/leaves', Payroll: '/payroll', Departments: '/departments', Settings: '/settings' };

export default function EmployeesPage({ user, onExit }) {
  const navigate = useNavigate(); const role = user?.user?.role || 'admin'; const canManage = role === 'admin';
  const [employees, setEmployees] = useState([]); const [departments, setDepartments] = useState([]); const [pagination, setPagination] = useState({ page: 1, totalPages: 1 }); const [filters, setFilters] = useState({ search: '', status: '', departmentId: '' }); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [modal, setModal] = useState(null); const [message, setMessage] = useState(''); const [mobileOpen, setMobileOpen] = useState(false);
  async function load(page = 1) { setLoading(true); setError(''); try { const [employeeResponse, departmentResponse] = await Promise.all([api.get('/employees', { params: cleanParams({ ...filters, page, pageSize: 10 }) }), api.get('/departments')]); setEmployees(employeeResponse.data.data?.items || []); setPagination(employeeResponse.data.data?.pagination || { page, totalPages: 1 }); setDepartments(departmentResponse.data.data || []); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not load employees.'); } finally { setLoading(false); } }
  useEffect(() => { load(1); }, [filters.search, filters.status, filters.departmentId]);
  function notify(text) { setMessage(text); window.setTimeout(() => setMessage(''), 2400); }
  async function submit(event) { event.preventDefault(); const form = new FormData(event.currentTarget); const values = Object.fromEntries(form.entries()); const payload = { ...values, departmentId: values.departmentId ? Number(values.departmentId) : null, role: values.role || 'employee', employmentType: values.employmentType || 'full-time' }; const salaryFields = ['baseSalary', 'houseAllowance', 'transportAllowance', 'medicalAllowance', 'taxDeduction', 'otherDeductions']; if (values.baseSalary) payload.salary = { effectiveFrom: values.salaryEffectiveFrom || new Date().toISOString().slice(0, 10), ...Object.fromEntries(salaryFields.filter(field => values[field] !== '').map(field => [field, Number(values[field])])) }; delete payload.baseSalary; delete payload.salaryEffectiveFrom; delete payload.houseAllowance; delete payload.transportAllowance; delete payload.medicalAllowance; delete payload.taxDeduction; delete payload.otherDeductions; if (modal.type === 'edit') delete payload.password; try { modal.type === 'create' ? await api.post('/employees', payload) : await api.patch(`/employees/${modal.employee.id}`, payload); notify(modal.type === 'create' ? 'Employee added successfully' : 'Employee updated successfully'); setModal(null); await load(pagination.page); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not save employee.'); } }
  async function changeStatus(event) { event.preventDefault(); const form = new FormData(event.currentTarget); try { await api.patch(`/employees/${modal.employee.id}/status`, { status: form.get('status'), reason: form.get('reason') || undefined }); notify('Employee status updated'); setModal(null); await load(pagination.page); } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Could not update status.'); } }
  const go = label => { setMobileOpen(false); navigate(paths[label] || '/dashboard'); };
  return <div className="app"><Sidebar active="People" role={role} open={mobileOpen} onClose={() => setMobileOpen(false)} onNavigate={go} onLogout={onExit} /><div className="app-content"><Topbar user={user} onMenu={() => setMobileOpen(true)} onNotifications={() => notify('You are all caught up')} onSettings={() => navigate('/settings')} onLogout={onExit} /><main className="dashboard-main"><Breadcrumbs items={[{ label: 'Workspace' }, { label: 'People' }]} /><div className="page-heading"><div><div className="eyebrow">Workspace</div><h1>People</h1><p>Search, filter, and manage your employee directory.</p></div>{canManage && <Button type="button" size="sm" onClick={() => setModal({ type: 'create' })}>+ Add employee</Button>}</div><div className="employee-filters"><input value={filters.search} onChange={event => setFilters(current => ({ ...current, search: event.target.value }))} placeholder="Search name or employee ID" /><select value={filters.departmentId} onChange={event => setFilters(current => ({ ...current, departmentId: event.target.value }))}><option value="">All departments</option>{departments.map(department => <option key={department.id} value={department.id}>{department.name}</option>)}</select><select value={filters.status} onChange={event => setFilters(current => ({ ...current, status: event.target.value }))}><option value="">All statuses</option><option value="active">Active</option><option value="on-leave">On leave</option><option value="terminated">Terminated</option></select></div>{error && <ErrorState message={error} onRetry={() => load(pagination.page)} />}{loading && !error && <LoadingState label="Loading employees…" />}{!loading && !error && <><div className="employee-table-wrap"><table className="data-table"><thead><tr><th>Employee</th><th>Employee ID</th><th>Department</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>{employees.map(employee => <tr key={employee.id}><td><button className="table-link" onClick={() => setModal({ type: 'view', employee })}>{employee.user?.name || employee.name}</button><small>{employee.user?.email || employee.email}</small></td><td>{employee.employeeCode}</td><td>{employee.department?.name || 'Unassigned'}</td><td>{employee.designation || employee.user?.role || 'Employee'}</td><td><StatusBadge status={employee.employmentStatus} /></td><td className="row-actions"><button onClick={() => setModal({ type: 'view', employee })}>View</button>{canManage && <><button onClick={() => setModal({ type: 'edit', employee })}>Edit</button><button onClick={() => setModal({ type: 'status', employee })}>Status</button></>}</td></tr>)}</tbody></table>{!employees.length && <div className="table-empty">No employees match the selected filters.</div>}</div><Pagination page={pagination.page || 1} totalPages={pagination.totalPages || 1} onChange={load} /></>}</main></div><Modal open={modal?.type === 'create' || modal?.type === 'edit'} title={modal?.type === 'edit' ? 'Edit employee' : 'Add employee'} description="Keep employee information and employment details up to date." onClose={() => setModal(null)}><EmployeeForm employee={modal?.employee} departments={departments} onSubmit={submit} edit={modal?.type === 'edit'} /></Modal><Modal open={modal?.type === 'status'} title="Change employment status" description="Status changes affect access and future HR workflows." onClose={() => setModal(null)}><form onSubmit={changeStatus}><label>Status<select name="status" defaultValue={modal?.employee?.employmentStatus || 'active'}><option value="active">Active</option><option value="on-leave">On leave</option><option value="terminated">Terminated</option></select></label><label>Reason<textarea name="reason" placeholder="Optional reason" /></label><Button type="submit" className="full">Update status</Button></form></Modal><Modal open={modal?.type === 'view'} title={modal?.employee?.user?.name || 'Employee profile'} description="Employee profile and employment information." onClose={() => setModal(null)}><EmployeeProfile employee={modal?.employee} /></Modal><Toast message={message} onClose={() => setMessage('')} /></div>;
}

function EmployeeForm({ employee, departments, onSubmit, edit }) { const salary = employee?.salaryStructures?.[0] || {}; return <form onSubmit={onSubmit}><label>Full name<input name="name" required defaultValue={employee?.user?.name || ''} /></label><label>Work email<input name="email" type="email" disabled={edit} required defaultValue={employee?.user?.email || ''} /></label>{!edit && <label>Temporary password<input name="password" type="password" minLength="8" required /></label>}<label>Role<select name="role" defaultValue={employee?.user?.role || 'employee'}><option value="employee">Employee</option><option value="manager">Manager</option></select></label><label>Department<select name="departmentId" defaultValue={employee?.departmentId || ''}><option value="">Unassigned</option>{departments.map(department => <option value={department.id} key={department.id}>{department.name}</option>)}</select></label><label>Job title<input name="designation" defaultValue={employee?.designation || ''} /></label><label>Employment type<select name="employmentType" defaultValue={employee?.employmentType || 'full-time'}><option value="full-time">Full-time</option><option value="part-time">Part-time</option><option value="contract">Contract</option></select></label><div className="form-section-title">Compensation</div><div className="form-grid"><label>Base salary<input name="baseSalary" type="number" min="0" step="0.01" defaultValue={salary.baseSalary || ''} /></label><label>Effective from<input name="salaryEffectiveFrom" type="date" defaultValue={salary.effectiveFrom || ''} /></label></div><div className="form-grid"><label>House allowance<input name="houseAllowance" type="number" min="0" step="0.01" defaultValue={salary.houseAllowance || ''} /></label><label>Transport allowance<input name="transportAllowance" type="number" min="0" step="0.01" defaultValue={salary.transportAllowance || ''} /></label></div><Button type="submit" className="full">{edit ? 'Save changes' : 'Add employee'}</Button></form>; }
function EmployeeProfile({ employee }) { const [documents, setDocuments] = useState([]); const [documentType, setDocumentType] = useState('other'); const [file, setFile] = useState(null); const [uploading, setUploading] = useState(false); const [documentError, setDocumentError] = useState(''); useEffect(() => { let mounted = true; api.get(`/employees/${employee.id}/documents`).then(response => { if (mounted) setDocuments(response.data.data || []); }).catch(() => { if (mounted) setDocumentError('Documents could not be loaded.'); }); return () => { mounted = false; }; }, [employee.id]); async function upload(event) { event.preventDefault(); if (!file) return; setUploading(true); setDocumentError(''); try { const body = new FormData(); body.append('file', file); body.append('documentType', documentType); const response = await api.post(`/employees/${employee.id}/documents`, body); setDocuments(current => [response.data.data, ...current]); setFile(null); event.currentTarget.reset(); } catch (error) { setDocumentError(error.response?.data?.error?.message || 'Document upload failed.'); } finally { setUploading(false); } } return <div className="employee-profile"><div className="profile-detail"><strong>Employee ID</strong><span>{employee?.employeeCode}</span></div><div className="profile-detail"><strong>Email</strong><span>{employee?.user?.email}</span></div><div className="profile-detail"><strong>Department</strong><span>{employee?.department?.name || 'Unassigned'}</span></div><div className="profile-detail"><strong>Job title</strong><span>{employee?.designation || 'Not specified'}</span></div><div className="profile-detail"><strong>Status</strong><StatusBadge status={employee?.employmentStatus} /></div>{employee?.salaryStructures?.length > 0 && <div className="salary-history"><h3>Salary history</h3>{employee.salaryStructures.map(salary => <div key={salary.id}><span>{salary.effectiveFrom}</span><strong>{salary.baseSalary}</strong></div>)}</div>}<div className="document-section"><h3>Documents</h3>{documentError && <p className="form-error">{documentError}</p>}<form onSubmit={upload} className="document-upload"><select value={documentType} onChange={event => setDocumentType(event.target.value)}><option value="other">Other</option><option value="contract">Contract</option><option value="cnic">CNIC</option><option value="resume">Resume</option></select><input type="file" required onChange={event => setFile(event.target.files?.[0] || null)} /><Button type="submit" loading={uploading}>Upload</Button></form>{documents.length ? <ul className="document-list">{documents.map(document => <li key={document.id}><span>{document.fileName || document.originalName || document.name}</span><small>{document.documentType}</small></li>)}</ul> : <p className="muted-text">No documents uploaded.</p>}</div></div>; }
